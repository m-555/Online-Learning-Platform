# Normalize line endings to LF in the repository for consistency across platforms.
* text=auto eol=lf

# Shell scripts must stay LF so they run inside Linux containers.
*.sh text eol=lf

# Treat lockfiles as generated (collapse diffs in reviews).
package-lock.json linguist-generated=true
